import { widthScale } from '../utils/scales.utility';

export const Typography = {
  heading1: widthScale(24),
  heading2: widthScale(20),
  body: widthScale(16),
  medium: widthScale(14),
  small: widthScale(12),
};
